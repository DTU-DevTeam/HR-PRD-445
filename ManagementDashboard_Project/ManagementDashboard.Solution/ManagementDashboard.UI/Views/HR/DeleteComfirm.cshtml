<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Xác nhận xóa</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f0f2f5;
            margin: 0;
            padding: 0;
            display: flex;
            height: 100vh;
            justify-content: center;
            margin-top: 30px;
        }

        .confirm-box {
            background: white;
            padding: 30px 40px;
            border-radius: 12px;
            box-shadow: 0 6px 18px rgba(0,0,0,0.1);
            max-width: 400px;
            text-align: center;
        }

            .confirm-box h3 {
                color: #333;
                margin-bottom: 25px;
                font-weight: 600;
            }

        .btn-group {
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        .btn {
            padding: 12px 28px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
            border: none;
            display: inline-block;
        }

        .btn-cancel {
            background: #e0e0e0;
            color: #555;
        }

            .btn-cancel:hover {
                background: #c6c6c6;
            }

        .btn-delete {
            background: #d9534f;
            color: white;
        }

            .btn-delete:hover {
                background: #c9302c;
            }
    </style>
</head>
<body>

    <div class="confirm-box">
        <h4>Are You Sure to Delete Employee ID: @ViewBag.EmployeeID?</h4>
        <div class="btn-group">
            <a href="~/hr/employees" class="btn btn-cancel">Cancel</a>
            <a asp-action="DeleteEmployee" asp-controller="hr" asp-route-id="@ViewBag.EmployeeID" class="btn btn-delete">Delete</a>
        </div>
    </div>

</body>
</html>

